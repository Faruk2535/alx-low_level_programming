#include <stdio.h>
#include "main.h"

/**
 *main= included a prototype function
 *
 *letter = prints letters in lowercase
 */
void print_alphabet(void)
{
  char letter;
    for(letter = 'a'; letter <= 'z'; letter++)
      {
	_putchar(letter);
      }

    _putchar('\n');
}
